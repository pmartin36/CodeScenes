using SceneBuilder.Authoring;

// See RouteAlpha.cs — the second compiled ISceneDefinition fixture for the multi-scene-builders
// router tests, so SceneBuilderRouter.Discover() has two distinct builders to enumerate.
public class RouteBeta : ISceneDefinition
{
    public void Build(SceneRoot scene)
    {
    }
}
