namespace SceneBuilder.Authoring
{
    public sealed partial class Between
    {
        // Nested enum FullName + member names/order are the write contract: they must equal
        // SceneBuilder.Core.Model.SpatialComponents.BetweenEnums byte-for-byte.
        public enum Axis { X, Y, Z }
    }
}
