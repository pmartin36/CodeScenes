namespace SceneBuilder.Authoring
{
    /// <summary>The reference frame an AlignTo alignment resolves edges and centers in: the
    /// target's own local axes, or world axes.</summary>
    public enum AlignSpace
    {
        TargetLocal,
        World,
    }
}
